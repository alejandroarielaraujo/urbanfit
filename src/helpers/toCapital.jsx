export function toCapital(str){
    return str.charAt(0).toUpperCase() + str.slice(1); // busca el primer caracter del string, lo convierte en mayuscula y suma al string original el resto de letras menos la primera
}
